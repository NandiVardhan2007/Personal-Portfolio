import { ContactPageClient } from './ContactPageClient';

export const metadata = {
    title: 'Contact',
    description: "Get in touch with Kovvuri Nandi Vardhan Reddy (Nandu) — open to internships, collaborations, and interesting problems.",
};

export default function ContactPage() {
    return <ContactPageClient />;
}
